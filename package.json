{
  "name": "magenvip",
  "version": "1.0.0",
  "description": "MagenVIP personal cyber shield — 3-language marketing site",
  "scripts": {
    "build": "eleventy",
    "start": "node assets/css/build.js && eleventy --serve",
    "prebuild": "node assets/css/build.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
